// automatically generated by flatc-rs, do not modify
// @generated

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export enum Color {
  Red = 1,
  Green = 2,
  Blue = 8
}

export class Inventory implements flatbuffers.IUnpackableObject<InventoryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;

  __init(i:number, bb:flatbuffers.ByteBuffer):Inventory {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsInventory(bb:flatbuffers.ByteBuffer, obj?:Inventory):Inventory {
    return (obj || new Inventory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getSizePrefixedRootAsInventory(bb:flatbuffers.ByteBuffer, obj?:Inventory):Inventory {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new Inventory()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getFullyQualifiedName(): "Inventory" {
    return 'Inventory';
  }

  items(index: number):number|null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.readInt32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
  }

  itemsArray():Int32Array|null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? new Int32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
  }

  itemsLength():number {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  names(index: number):string
  names(index: number, optionalEncoding:flatbuffers.Encoding):string|Uint8Array
  names(index: number, optionalEncoding?:any):string|Uint8Array|null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__string(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), optionalEncoding) : null;
  }

  namesLength():number {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  colors(index: number):Color|null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.readInt8(this.bb!.__vector(this.bb_pos + offset) + index * 1) : 0;
  }

  colorsArray():Int8Array|null {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? new Int8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
  }

  colorsLength():number {
    const offset = this.bb!.__offset(this.bb_pos, 8);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  static startInventory(builder:flatbuffers.Builder) {
    builder.startObject(3);
  }

  static addItems(builder:flatbuffers.Builder, itemsOffset:flatbuffers.Offset) {
    builder.addFieldOffset(0, itemsOffset, 0);
  }

  static createItemsVector(builder:flatbuffers.Builder, data:number[]|Int32Array):flatbuffers.Offset {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt32(data[i]!);
    }
    return builder.endVector();
  }

  static startItemsVector(builder:flatbuffers.Builder, numElems:number) {
    builder.startVector(4, numElems, 4);
  }

  static addNames(builder:flatbuffers.Builder, namesOffset:flatbuffers.Offset) {
    builder.addFieldOffset(1, namesOffset, 0);
  }

  static startNamesVector(builder:flatbuffers.Builder, numElems:number) {
    builder.startVector(4, numElems, 4);
  }

  static addColors(builder:flatbuffers.Builder, colorsOffset:flatbuffers.Offset) {
    builder.addFieldOffset(2, colorsOffset, 0);
  }

  static createColorsVector(builder:flatbuffers.Builder, data:number[]|Int8Array):flatbuffers.Offset {
    builder.startVector(1, data.length, 1);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt8(data[i]!);
    }
    return builder.endVector();
  }

  static startColorsVector(builder:flatbuffers.Builder, numElems:number) {
    builder.startVector(1, numElems, 1);
  }

  static endInventory(builder:flatbuffers.Builder):flatbuffers.Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createInventory(builder:flatbuffers.Builder, items:flatbuffers.Offset, names:flatbuffers.Offset, colors:flatbuffers.Offset):flatbuffers.Offset {
    Inventory.startInventory(builder);
    Inventory.addItems(builder, items);
    Inventory.addNames(builder, names);
    Inventory.addColors(builder, colors);
    return Inventory.endInventory(builder);
  }

  serialize():Uint8Array {
    return this.bb!.bytes();
  }

  static deserialize(buffer: Uint8Array):Inventory {
    return Inventory.getRootAsInventory(new flatbuffers.ByteBuffer(buffer))
  }

  unpack():InventoryT {
    return new InventoryT(
      this.bb!.createScalarList<number>(this.items.bind(this), this.itemsLength()),
      this.bb!.createScalarList<string>(this.names.bind(this), this.namesLength()),
      this.bb!.createScalarList<number>(this.colors.bind(this), this.colorsLength())
    );
  }

  unpackTo(_o:InventoryT):void {
    _o.items = this.bb!.createScalarList<number>(this.items.bind(this), this.itemsLength());
    _o.names = this.bb!.createScalarList<string>(this.names.bind(this), this.namesLength());
    _o.colors = this.bb!.createScalarList<number>(this.colors.bind(this), this.colorsLength());
  }
}

export class InventoryT implements flatbuffers.IGeneratedObject {
  constructor(
    public items:(number)[] = [],
    public names:(string)[] = [],
    public colors:(Color)[] = []
  ) {}

  pack(builder:flatbuffers.Builder):flatbuffers.Offset {
    const items = Inventory.createItemsVector(builder, this.items);
    let namesOffset = 0;
    if (this.names) {
      const offsets: flatbuffers.Offset[] = [];
      for (const s of this.names) {
        offsets.push(builder.createString(s));
      }
      Inventory.startNamesVector(builder, offsets.length);
      for (let i = offsets.length - 1; i >= 0; i--) {
        builder.addOffset(offsets[i]!);
      }
      namesOffset = builder.endVector();
    }
    const names = namesOffset;
    const colors = Inventory.createColorsVector(builder, this.colors);

    Inventory.startInventory(builder);
    Inventory.addItems(builder, items);
    Inventory.addNames(builder, names);
    Inventory.addColors(builder, colors);
    return Inventory.endInventory(builder);
  }
}
