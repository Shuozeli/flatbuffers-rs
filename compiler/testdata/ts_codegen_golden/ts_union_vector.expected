// automatically generated by flatc-rs, do not modify
// @generated

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export enum Pet {
  NONE = 0,
  Cat = 1,
  Tag = 2,
  Label = 3
}

export function unionToPet(
  type: Pet,
  accessor: (obj:Cat|Tag|string) => Cat|Tag|string|null
): Cat|Tag|string|null {
  switch(Pet[type]) {
    case 'NONE': return null;
    case 'Cat': return accessor(new Cat())! as Cat;
    case 'Tag': return accessor(new Tag())! as Tag;
    case 'Label': return accessor('') as string;
    default: return null;
  }
}

export function unionListToPet(
  type: Pet,
  accessor: (index: number, obj:Cat|Tag|string) => Cat|Tag|string|null,
  index: number
): Cat|Tag|string|null {
  switch(Pet[type]) {
    case 'NONE': return null;
    case 'Cat': return accessor(index, new Cat())! as Cat;
    case 'Tag': return accessor(index, new Tag())! as Tag;
    case 'Label': return accessor(index, '') as string;
    default: return null;
  }
}

export class PetT implements flatbuffers.IGeneratedObject {
  constructor(
  public type: Pet = Pet.NONE,
  public value: CatT|TagT|string|null = null
) {}

  pack(builder:flatbuffers.Builder): flatbuffers.Offset {
    switch(this.type) {
      case Pet.NONE: return 0;
      case Pet.Cat: return (this.value as CatT).pack(builder);
      case Pet.Tag: return (this.value as TagT).pack(builder);
      case Pet.Label: return builder.createString(this.value as string);
      default: return 0;
    }
  }
}

export class Cat implements flatbuffers.IUnpackableObject<CatT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;

  __init(i:number, bb:flatbuffers.ByteBuffer):Cat {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsCat(bb:flatbuffers.ByteBuffer, obj?:Cat):Cat {
    return (obj || new Cat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getSizePrefixedRootAsCat(bb:flatbuffers.ByteBuffer, obj?:Cat):Cat {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new Cat()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getFullyQualifiedName(): "Cat" {
    return 'Cat';
  }

  name():string|null
  name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
  name(optionalEncoding?:any):string|Uint8Array|null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
  }

  static startCat(builder:flatbuffers.Builder) {
    builder.startObject(1);
  }

  static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
    builder.addFieldOffset(0, nameOffset, 0);
  }

  static endCat(builder:flatbuffers.Builder):flatbuffers.Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createCat(builder:flatbuffers.Builder, name:flatbuffers.Offset):flatbuffers.Offset {
    Cat.startCat(builder);
    Cat.addName(builder, name);
    return Cat.endCat(builder);
  }

  serialize():Uint8Array {
    return this.bb!.bytes();
  }

  static deserialize(buffer: Uint8Array):Cat {
    return Cat.getRootAsCat(new flatbuffers.ByteBuffer(buffer))
  }

  unpack():CatT {
    return new CatT(
      this.name()
    );
  }

  unpackTo(_o:CatT):void {
    _o.name = this.name();
  }
}

export class CatT implements flatbuffers.IGeneratedObject {
  constructor(
    public name:string|null = null
  ) {}

  pack(builder:flatbuffers.Builder):flatbuffers.Offset {
    const name = (this.name !== null ? builder.createString(this.name!) : 0);

    Cat.startCat(builder);
    Cat.addName(builder, name);
    return Cat.endCat(builder);
  }
}

export class Tag implements flatbuffers.IUnpackableObject<TagT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;

  __init(i:number, bb:flatbuffers.ByteBuffer):Tag {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getFullyQualifiedName(): "Tag" {
    return 'Tag';
  }

  value():number {
    return this.bb!.readInt32(this.bb_pos + 0);
  }

  mutateValue(value:number):boolean {
    this.bb!.writeInt32(this.bb_pos + 0, value);
    return true;
  }

  static sizeOf():number {
    return 4;
  }

  static createTag(builder:flatbuffers.Builder, value:number):flatbuffers.Offset {
    builder.prep(4, 4);
    builder.writeInt32(value);
    return builder.offset();
  }

  unpack():TagT {
    return new TagT(
      this.value()
    );
  }

  unpackTo(_o:TagT):void {
    _o.value = this.value();
  }
}

export class TagT implements flatbuffers.IGeneratedObject {
  constructor(
    public value:number = 0
  ) {}

  pack(builder:flatbuffers.Builder):flatbuffers.Offset {
    return Tag.createTag(builder, this.value);
  }
}

export class Zoo implements flatbuffers.IUnpackableObject<ZooT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;

  __init(i:number, bb:flatbuffers.ByteBuffer):Zoo {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }

  static getRootAsZoo(bb:flatbuffers.ByteBuffer, obj?:Zoo):Zoo {
    return (obj || new Zoo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getSizePrefixedRootAsZoo(bb:flatbuffers.ByteBuffer, obj?:Zoo):Zoo {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new Zoo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }

  static getFullyQualifiedName(): "Zoo" {
    return 'Zoo';
  }

  petsType(index: number):Pet|null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index * 1) : 0;
  }

  petsTypeArray():Uint8Array|null {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
  }

  petsTypeLength():number {
    const offset = this.bb!.__offset(this.bb_pos, 4);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  pets(index: number, obj:any|string):any|string|null {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__union_with_string(obj, this.bb!.__vector(this.bb_pos + offset) + index * 4) : null;
  }

  petsLength():number {
    const offset = this.bb!.__offset(this.bb_pos, 6);
    return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
  }

  static startZoo(builder:flatbuffers.Builder) {
    builder.startObject(2);
  }

  static addPetsType(builder:flatbuffers.Builder, petsTypeOffset:flatbuffers.Offset) {
    builder.addFieldOffset(0, petsTypeOffset, 0);
  }

  static createPetsTypeVector(builder:flatbuffers.Builder, data:Pet[]|Uint8Array):flatbuffers.Offset {
    builder.startVector(1, data.length, 1);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addInt8(data[i]!);
    }
    return builder.endVector();
  }

  static startPetsTypeVector(builder:flatbuffers.Builder, numElems:number) {
    builder.startVector(1, numElems, 1);
  }

  static addPets(builder:flatbuffers.Builder, petsOffset:flatbuffers.Offset) {
    builder.addFieldOffset(1, petsOffset, 0);
  }

  static startPetsVector(builder:flatbuffers.Builder, numElems:number) {
    builder.startVector(4, numElems, 4);
  }

  static endZoo(builder:flatbuffers.Builder):flatbuffers.Offset {
    const offset = builder.endObject();
    return offset;
  }

  static createZoo(builder:flatbuffers.Builder, petsType:flatbuffers.Offset, pets:flatbuffers.Offset):flatbuffers.Offset {
    Zoo.startZoo(builder);
    Zoo.addPetsType(builder, petsType);
    Zoo.addPets(builder, pets);
    return Zoo.endZoo(builder);
  }

  serialize():Uint8Array {
    return this.bb!.bytes();
  }

  static deserialize(buffer: Uint8Array):Zoo {
    return Zoo.getRootAsZoo(new flatbuffers.ByteBuffer(buffer))
  }

  unpack():ZooT {
    return new ZooT(
      this.bb!.createScalarList<Pet>(this.petsType.bind(this), this.petsTypeLength()),
      (() => { const values:(CatT|TagT|string|null)[] = []; for (let i = 0; i < this.petsLength(); i++) { const type = this.petsType(i); if (type === null || type === Pet.NONE) { values.push(null); continue; } const value = unionListToPet(type, (index:number, obj:any) => this.pets(index, obj), i); values.push(value === null ? null : (typeof value === 'string' ? value : value.unpack())); } return values; })()
    );
  }

  unpackTo(_o:ZooT):void {
    _o.petsType = this.bb!.createScalarList<Pet>(this.petsType.bind(this), this.petsTypeLength());
    _o.pets = (() => { const values:(CatT|TagT|string|null)[] = []; for (let i = 0; i < this.petsLength(); i++) { const type = this.petsType(i); if (type === null || type === Pet.NONE) { values.push(null); continue; } const value = unionListToPet(type, (index:number, obj:any) => this.pets(index, obj), i); values.push(value === null ? null : (typeof value === 'string' ? value : value.unpack())); } return values; })();
  }
}

export class ZooT implements flatbuffers.IGeneratedObject {
  constructor(
    public petsType:(Pet)[] = [],
    public pets:(CatT|TagT|string|null)[] = []
  ) {}

  pack(builder:flatbuffers.Builder):flatbuffers.Offset {
    const petsType = Zoo.createPetsTypeVector(builder, this.petsType);
    if (this.petsType.length !== this.pets.length) {
      throw new Error('Zoo.pets union type and value vectors must have equal lengths');
    }
    const offsets: flatbuffers.Offset[] = [];
    for (let i = 0; i < this.pets.length; i++) {
      const type = this.petsType[i]!;
      const item = this.pets[i];
      if (type === Pet.NONE) {
        if (item !== null) {
          throw new Error('Zoo.pets requires null for a NONE union element');
        }
        offsets.push(0);
        continue;
      }
      if (item === null) {
        throw new Error('Zoo.pets requires a value for a non-NONE union element');
      }
      offsets.push(typeof item === 'string' ? builder.createString(item) : item.pack(builder));
    }
    Zoo.startPetsVector(builder, offsets.length);
    for (let i = offsets.length - 1; i >= 0; i--) {
      const offset = offsets[i]!;
      if (offset === 0) builder.addInt32(0); else builder.addOffset(offset);
    }
    const pets = builder.endVector();

    Zoo.startZoo(builder);
    Zoo.addPetsType(builder, petsType);
    Zoo.addPets(builder, pets);
    return Zoo.endZoo(builder);
  }
}
