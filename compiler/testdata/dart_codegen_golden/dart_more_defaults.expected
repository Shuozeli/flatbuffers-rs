// automatically generated by flatc-rs, do not modify
// ignore_for_file: unused_import, unused_field, unused_element, unused_local_variable

library;

import 'dart:typed_data' show Uint8List;

import 'package:flat_buffers/flat_buffers.dart' as fb;


class ABC {
  final int value;
  const ABC._(this.value);

  factory ABC.fromValue(int value) {
    final result = values[value];
    if (result == null) {
      throw StateError('Invalid value $value for bit flag enum ABC',
      );
    }
    return result;
  }

  static ABC? _createOrNull(int? value) =>
    value == null ? null : ABC.fromValue(value);

  static const ABC A = ABC._(0);
  static const ABC B = ABC._(1);
  static const ABC C = ABC._(2);

  static const Map<int, ABC> values = {
    0: A,
    1: B,
    2: C,
  };

  static const fb.Reader<ABC> reader = _ABCReader();

  @override
  String toString() {
    return 'ABC{value: $value}';
  }
}

class _ABCReader extends fb.Reader<ABC> {
  const _ABCReader();

  @override
  int get size => 1;

  @override
  ABC read(fb.BufferContext bc, int offset) =>
    ABC.fromValue(const fb.Int8Reader().read(bc, offset));
}

class MoreDefaults {
  MoreDefaults._(this._bc, this._bcOffset);

  factory MoreDefaults(List<int> bytes) {
    final rootRef = fb.BufferContext.fromBytes(bytes);
    return reader.read(rootRef, 0);
  }

  static const fb.Reader<MoreDefaults> reader = _MoreDefaultsReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int? get ints => const fb.Int32Reader().vTableGetNullable(_bc, _bcOffset, 4);

  double? get floats => const fb.Float32Reader().vTableGetNullable(_bc, _bcOffset, 6);

  String? get emptyString => const fb.StringReader().vTableGetNullable(_bc, _bcOffset, 8);

  String get someString => const fb.StringReader().vTableGet(_bc, _bcOffset, 10, defaultValue: 'some') ?? '';

  ABC? get abcs => const fb.Int32Reader().vTableGetNullable(_bc, _bcOffset, 12);

  bool? get bools => const fb.Int8Reader().vTableGetNullable(_bc, _bcOffset, 14);

  @override
  String toString() {
    return 'MoreDefaults{ints: ${ints}, floats: ${floats}, emptyString: ${emptyString}, someString: ${someString}, abcs: ${abcs}, bools: ${bools}}';
  }

  MoreDefaultsT unpack() => MoreDefaultsT(ints: ints?.unpack(), floats: floats?.unpack(), emptyString: emptyString?.unpack(), someString: someString?.unpack(), abcs: abcs?.unpack(), bools: bools?.unpack());

  static int pack(fb.Builder fbBuilder, MoreDefaultsT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class _MoreDefaultsReader extends fb.TableReader<MoreDefaults> {
  const _MoreDefaultsReader();

  @override
  MoreDefaults createObject(fb.BufferContext bc, int offset) =>
    MoreDefaults._(bc, offset);
}

class MoreDefaultsBuilder {
  MoreDefaultsBuilder(this.fbBuilder);

  final fb.Builder fbBuilder;

  void begin() {
    fbBuilder.startTable(6);
  }

  int addIntsOffset(int? intsOffset) {
    fbBuilder.addOffset(0, intsOffset ?? 0);
    return fbBuilder.offset;
  }

  int addFloatsOffset(int? floatsOffset) {
    fbBuilder.addOffset(1, floatsOffset ?? 0);
    return fbBuilder.offset;
  }

  int addEmptyStringOffset(int? emptyStringOffset) {
    fbBuilder.addOffset(2, emptyStringOffset ?? 0);
    return fbBuilder.offset;
  }

  int addSomeStringOffset(int? someStringOffset) {
    fbBuilder.addOffset(3, someStringOffset ?? 0);
    return fbBuilder.offset;
  }

  int addAbcsOffset(int? abcsOffset) {
    fbBuilder.addOffset(4, abcsOffset ?? 0);
    return fbBuilder.offset;
  }

  int addBoolsOffset(int? boolsOffset) {
    fbBuilder.addOffset(5, boolsOffset ?? 0);
    return fbBuilder.offset;
  }

  int finish() {
    return fbBuilder.endTable();
  }
}

class MoreDefaultsT implements fb.Packable {
  (int)? ints;
  (double)? floats;
  String? emptyString;
  String? someString;
  (ABC)? abcs;
  (bool)? bools;

  MoreDefaultsT({
    this.ints,
    this.floats,
    this.emptyString,
    this.someString,
    this.abcs,
    this.bools
  });

  @override
  int pack(fb.Builder fbBuilder) {
    final int? intsOffset = ints?.pack(fbBuilder);
    final int? floatsOffset = floats?.pack(fbBuilder);
    final int emptyStringOffset = emptyString != null ? fbBuilder.writeString(emptyString) : 0;
    final int someStringOffset = someString != null ? fbBuilder.writeString(someString) : 0;
    final int? abcsOffset = abcs?.pack(fbBuilder);
    final int? boolsOffset = bools?.pack(fbBuilder);
    fbBuilder.startTable(6);
    fbBuilder.addOffset(0, intsOffset ?? 0);
    fbBuilder.addOffset(1, floatsOffset ?? 0);
    fbBuilder.addOffset(2, emptyStringOffset);
    fbBuilder.addOffset(3, someStringOffset);
    fbBuilder.addOffset(4, abcsOffset ?? 0);
    fbBuilder.addOffset(5, boolsOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  String toString() {
    return 'MoreDefaultsT{ints: ${ints}, floats: ${floats}, emptyString: ${emptyString}, someString: ${someString}, abcs: ${abcs}, bools: ${bools}}';
  }
}

class MoreDefaultsObjectBuilder extends fb.ObjectBuilder {
  final int? _ints;
  final int? _floats;
  final String? _emptyString;
  final String? _someString;
  final int? _abcs;
  final int? _bools;

  MoreDefaultsObjectBuilder({
    this._ints,
    this._floats,
    this._emptyString,
    this._someString,
    this._abcs,
    this._bools,
  });

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.startTable(6);
    final int? intsOffset = _ints?.pack(fbBuilder);
    fbBuilder.addOffset(0, intsOffset ?? 0);
    final int? floatsOffset = _floats?.pack(fbBuilder);
    fbBuilder.addOffset(1, floatsOffset ?? 0);
    final int emptyStringOffset = _emptyString != null ? fbBuilder.writeString(_emptyString) : 0;
    fbBuilder.addOffset(2, emptyStringOffset);
    final int someStringOffset = _someString != null ? fbBuilder.writeString(_someString) : 0;
    fbBuilder.addOffset(3, someStringOffset);
    final int? abcsOffset = _abcs?.pack(fbBuilder);
    fbBuilder.addOffset(4, abcsOffset ?? 0);
    final int? boolsOffset = _bools?.pack(fbBuilder);
    fbBuilder.addOffset(5, boolsOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}
