// automatically generated by flatc-rs, do not modify
// ignore_for_file: unused_import, unused_field, unused_element, unused_local_variable

library;

import 'dart:typed_data' show Uint8List;

import 'package:flat_buffers/flat_buffers.dart' as fb;


class Inventory {
  Inventory._(this._bc, this._bcOffset);

  factory Inventory(List<int> bytes) {
    final rootRef = fb.BufferContext.fromBytes(bytes);
    return reader.read(rootRef, 0);
  }

  static const fb.Reader<Inventory> reader = _InventoryReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int? get slots => const fb.Uint8Reader().vTableGetNullable(_bc, _bcOffset, 4);

  String? get names => const fb.StringReader().vTableGetNullable(_bc, _bcOffset, 6);

  int? get scores => const fb.Int32Reader().vTableGetNullable(_bc, _bcOffset, 8);

  @override
  String toString() {
    return 'Inventory{slots: ${slots}, names: ${names}, scores: ${scores}}';
  }

  InventoryT unpack() => InventoryT(slots: slots?.unpack(), names: names?.unpack(), scores: scores?.unpack());

  static int pack(fb.Builder fbBuilder, InventoryT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class _InventoryReader extends fb.TableReader<Inventory> {
  const _InventoryReader();

  @override
  Inventory createObject(fb.BufferContext bc, int offset) =>
    Inventory._(bc, offset);
}

class InventoryBuilder {
  InventoryBuilder(this.fbBuilder);

  final fb.Builder fbBuilder;

  void begin() {
    fbBuilder.startTable(3);
  }

  int addSlotsOffset(int? slotsOffset) {
    fbBuilder.addOffset(0, slotsOffset ?? 0);
    return fbBuilder.offset;
  }

  int addNamesOffset(int? namesOffset) {
    fbBuilder.addOffset(1, namesOffset ?? 0);
    return fbBuilder.offset;
  }

  int addScoresOffset(int? scoresOffset) {
    fbBuilder.addOffset(2, scoresOffset ?? 0);
    return fbBuilder.offset;
  }

  int finish() {
    return fbBuilder.endTable();
  }
}

class InventoryT implements fb.Packable {
  (int)? slots;
  (String)? names;
  (int)? scores;

  InventoryT({
    this.slots,
    this.names,
    this.scores
  });

  @override
  int pack(fb.Builder fbBuilder) {
    final int? slotsOffset = slots?.pack(fbBuilder);
    final int? namesOffset = names?.pack(fbBuilder);
    final int? scoresOffset = scores?.pack(fbBuilder);
    fbBuilder.startTable(3);
    fbBuilder.addOffset(0, slotsOffset ?? 0);
    fbBuilder.addOffset(1, namesOffset ?? 0);
    fbBuilder.addOffset(2, scoresOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  String toString() {
    return 'InventoryT{slots: ${slots}, names: ${names}, scores: ${scores}}';
  }
}

class InventoryObjectBuilder extends fb.ObjectBuilder {
  final int? _slots;
  final int? _names;
  final int? _scores;

  InventoryObjectBuilder({
    this._slots,
    this._names,
    this._scores,
  });

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.startTable(3);
    final int? slotsOffset = _slots?.pack(fbBuilder);
    fbBuilder.addOffset(0, slotsOffset ?? 0);
    final int? namesOffset = _names?.pack(fbBuilder);
    fbBuilder.addOffset(1, namesOffset ?? 0);
    final int? scoresOffset = _scores?.pack(fbBuilder);
    fbBuilder.addOffset(2, scoresOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}
