// automatically generated by flatc-rs, do not modify
// ignore_for_file: unused_import, unused_field, unused_element, unused_local_variable

library;

import 'dart:typed_data' show Uint8List;

import 'package:flat_buffers/flat_buffers.dart' as fb;


class Inner {
  Inner._(this._bc, this._bcOffset);

  factory Inner(List<int> bytes) {
    final rootRef = fb.BufferContext.fromBytes(bytes);
    return reader.read(rootRef, 0);
  }

  static const fb.Reader<Inner> reader = _InnerReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int get hp => const fb.Int32Reader().vTableGet(_bc, _bcOffset, 4, 100);

  String? get name => const fb.StringReader().vTableGetNullable(_bc, _bcOffset, 6);

  @override
  String toString() {
    return 'Inner{hp: ${hp}, name: ${name}}';
  }

  InnerT unpack() => InnerT(hp: hp?.unpack(), name: name?.unpack());

  static int pack(fb.Builder fbBuilder, InnerT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class _InnerReader extends fb.TableReader<Inner> {
  const _InnerReader();

  @override
  Inner createObject(fb.BufferContext bc, int offset) =>
    Inner._(bc, offset);
}

class InnerBuilder {
  InnerBuilder(this.fbBuilder);

  final fb.Builder fbBuilder;

  void begin() {
    fbBuilder.startTable(2);
  }

  int addHp(int hp) {
    fbBuilder.addInt32(0, hp);
    return fbBuilder.offset;
  }

  int addNameOffset(int? nameOffset) {
    fbBuilder.addOffset(1, nameOffset ?? 0);
    return fbBuilder.offset;
  }

  int finish() {
    return fbBuilder.endTable();
  }
}

class InnerT implements fb.Packable {
  int hp;
  String? name;

  InnerT({
    this.hp = 0,
    this.name
  });

  @override
  int pack(fb.Builder fbBuilder) {
    final int nameOffset = name != null ? fbBuilder.writeString(name) : 0;
    fbBuilder.startTable(2);
    fbBuilder.addInt32(0, hp);
    fbBuilder.addOffset(1, nameOffset);
    return fbBuilder.endTable();
  }

  @override
  String toString() {
    return 'InnerT{hp: ${hp}, name: ${name}}';
  }
}

class InnerObjectBuilder extends fb.ObjectBuilder {
  final int _hp;
  final String? _name;

  InnerObjectBuilder({
    this._hp,
    this._name,
  });

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.startTable(2);
    fbBuilder.addInt32(0, _hp);
    final int nameOffset = _name != null ? fbBuilder.writeString(_name) : 0;
    fbBuilder.addOffset(1, nameOffset);
    return fbBuilder.endTable();
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}

class Outer {
  Outer._(this._bc, this._bcOffset);

  factory Outer(List<int> bytes) {
    final rootRef = fb.BufferContext.fromBytes(bytes);
    return reader.read(rootRef, 0);
  }

  static const fb.Reader<Outer> reader = _OuterReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  String? get label => const fb.StringReader().vTableGetNullable(_bc, _bcOffset, 4);

  int? get data => const fb.Uint8Reader().vTableGetNullable(_bc, _bcOffset, 6);

  @override
  String toString() {
    return 'Outer{label: ${label}, data: ${data}}';
  }

  OuterT unpack() => OuterT(label: label?.unpack(), data: data?.unpack());

  static int pack(fb.Builder fbBuilder, OuterT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class _OuterReader extends fb.TableReader<Outer> {
  const _OuterReader();

  @override
  Outer createObject(fb.BufferContext bc, int offset) =>
    Outer._(bc, offset);
}

class OuterBuilder {
  OuterBuilder(this.fbBuilder);

  final fb.Builder fbBuilder;

  void begin() {
    fbBuilder.startTable(2);
  }

  int addLabelOffset(int? labelOffset) {
    fbBuilder.addOffset(0, labelOffset ?? 0);
    return fbBuilder.offset;
  }

  int addDataOffset(int? dataOffset) {
    fbBuilder.addOffset(1, dataOffset ?? 0);
    return fbBuilder.offset;
  }

  int finish() {
    return fbBuilder.endTable();
  }
}

class OuterT implements fb.Packable {
  String? label;
  (int)? data;

  OuterT({
    this.label,
    this.data
  });

  @override
  int pack(fb.Builder fbBuilder) {
    final int labelOffset = label != null ? fbBuilder.writeString(label) : 0;
    final int? dataOffset = data?.pack(fbBuilder);
    fbBuilder.startTable(2);
    fbBuilder.addOffset(0, labelOffset);
    fbBuilder.addOffset(1, dataOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  String toString() {
    return 'OuterT{label: ${label}, data: ${data}}';
  }
}

class OuterObjectBuilder extends fb.ObjectBuilder {
  final String? _label;
  final int? _data;

  OuterObjectBuilder({
    this._label,
    this._data,
  });

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.startTable(2);
    final int labelOffset = _label != null ? fbBuilder.writeString(_label) : 0;
    fbBuilder.addOffset(0, labelOffset);
    final int? dataOffset = _data?.pack(fbBuilder);
    fbBuilder.addOffset(1, dataOffset ?? 0);
    return fbBuilder.endTable();
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}
