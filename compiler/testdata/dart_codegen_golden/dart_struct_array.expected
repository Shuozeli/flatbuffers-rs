// automatically generated by flatc-rs, do not modify
// ignore_for_file: unused_import, unused_field, unused_element, unused_local_variable

library;

import 'dart:typed_data' show Uint8List;

import 'package:flat_buffers/flat_buffers.dart' as fb;


class TestEnum {
  final int value;
  const TestEnum._(this.value);

  factory TestEnum.fromValue(int value) {
    final result = values[value];
    if (result == null) {
      throw StateError('Invalid value $value for bit flag enum TestEnum',
      );
    }
    return result;
  }

  static TestEnum? _createOrNull(int? value) =>
    value == null ? null : TestEnum.fromValue(value);

  static const TestEnum A = TestEnum._(0);
  static const TestEnum B = TestEnum._(1);
  static const TestEnum C = TestEnum._(2);

  static const Map<int, TestEnum> values = {
    0: A,
    1: B,
    2: C,
  };

  static const fb.Reader<TestEnum> reader = _TestEnumReader();

  @override
  String toString() {
    return 'TestEnum{value: $value}';
  }
}

class _TestEnumReader extends fb.Reader<TestEnum> {
  const _TestEnumReader();

  @override
  int get size => 1;

  @override
  TestEnum read(fb.BufferContext bc, int offset) =>
    TestEnum.fromValue(const fb.Int8Reader().read(bc, offset));
}

class Inner {
  Inner._(this._bc, this._bcOffset);

  static const fb.Reader<Inner> reader = _InnerReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int get x => const fb.Int32Reader().read(_bc, _bcOffset + 0);

  int get y => const fb.Int32Reader().read(_bc, _bcOffset + 4);

  @override
  String toString() {
    return 'Inner{x: ${x}, y: ${y}}';
  }

  InnerT unpack() => InnerT(x: x, y: y);

  static int pack(fb.Builder fbBuilder, InnerT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class InnerT implements fb.Packable {
  int x;
  int y;

  InnerT(
  {
    this.x = 0,
    this.y = 0
  });

  @override
  int pack(fb.Builder fbBuilder) {
    fbBuilder.putInt32(y);
    fbBuilder.putInt32(x);
    return fbBuilder.offset;
  }

  @override
  String toString() {
    return 'InnerT{x: ${x}, y: ${y}}';
  }
}

class _InnerReader extends fb.StructReader<Inner> {
  const _InnerReader();

  @override
  int get size => 8;

  @override
  Inner createObject(fb.BufferContext bc, int offset) =>
    Inner._(bc, offset);
}

class InnerBuilder {
  InnerBuilder(this.fbBuilder, this._x, this._y);

  final fb.Builder fbBuilder;
  final int _x;
  final int _y;

  int finish() {
    fbBuilder.putInt32(_y);
    fbBuilder.putInt32(_x);
    return fbBuilder.offset;
  }
}

class InnerObjectBuilder extends fb.ObjectBuilder {
  final int _x;
  final int _y;

  InnerObjectBuilder(
    this._x,
    this._y
  );

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.putInt32(_y);
    fbBuilder.putInt32(_x);
    return fbBuilder.offset;
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}

class Outer {
  Outer._(this._bc, this._bcOffset);

  static const fb.Reader<Outer> reader = _OuterReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int? get a => const fb.Int32Reader().vTableGetNullable(_bc, _bcOffset, 0);

  TestEnum get b => const fb.Int8Reader().read(_bc, _bcOffset + 12);

  TestEnum? get c => const fb.Int8Reader().vTableGetNullable(_bc, _bcOffset, 13);

  Inner? get d => Inner.reader.vTableGetNullable(
    _bc, _bcOffset, 16);

  double get e => const fb.Float32Reader().read(_bc, _bcOffset + 32);

  @override
  String toString() {
    return 'Outer{a: ${a}, b: ${b}, c: ${c}, d: ${d}, e: ${e}}';
  }

  OuterT unpack() => OuterT(a: a, b: b, c: c, d: d, e: e);

  static int pack(fb.Builder fbBuilder, OuterT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class OuterT implements fb.Packable {
  (int)? a;
  TestEnum b;
  (TestEnum)? c;
  (InnerT)? d;
  double e;

  OuterT(
  {
    this.a,
    this.b = 0,
    this.c,
    this.d,
    this.e = 0.0
  });

  @override
  int pack(fb.Builder fbBuilder) {
    fbBuilder.putFloat32(e);
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt32(d?[i]?.pack(fbBuilder) ?? 0);
    }
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt8(c?[i] ?? 0);
    }
    fbBuilder.putInt8(b);
    for (int i = 3 - 1; i >= 0; i--) {
      fbBuilder.putInt32(a?[i] ?? 0);
    }
    return fbBuilder.offset;
  }

  @override
  String toString() {
    return 'OuterT{a: ${a}, b: ${b}, c: ${c}, d: ${d}, e: ${e}}';
  }
}

class _OuterReader extends fb.StructReader<Outer> {
  const _OuterReader();

  @override
  int get size => 36;

  @override
  Outer createObject(fb.BufferContext bc, int offset) =>
    Outer._(bc, offset);
}

class OuterBuilder {
  OuterBuilder(this.fbBuilder, this._a, this._b, this._c, this._d, this._e);

  final fb.Builder fbBuilder;
  final int? _a;
  final TestEnum _b;
  final TestEnum? _c;
  final Inner? _d;
  final double _e;

  int finish() {
    fbBuilder.putFloat32(_e);
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt32(_d?[i]?.pack(fbBuilder) ?? 0);
    }
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt8(_c?[i] ?? 0);
    }
    fbBuilder.putInt8(_b);
    for (int i = 3 - 1; i >= 0; i--) {
      fbBuilder.putInt32(_a?[i] ?? 0);
    }
    return fbBuilder.offset;
  }
}

class OuterObjectBuilder extends fb.ObjectBuilder {
  final int? _a;
  final TestEnum _b;
  final TestEnum? _c;
  final Inner? _d;
  final double _e;

  OuterObjectBuilder(
    this._a,
    this._b,
    this._c,
    this._d,
    this._e
  );

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.putFloat32(_e);
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt32(_d?[i]?.pack(fbBuilder) ?? 0);
    }
    for (int i = 2 - 1; i >= 0; i--) {
      fbBuilder.putInt8(_c?[i] ?? 0);
    }
    fbBuilder.putInt8(_b);
    for (int i = 3 - 1; i >= 0; i--) {
      fbBuilder.putInt32(_a?[i] ?? 0);
    }
    return fbBuilder.offset;
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}
