// automatically generated by flatc-rs, do not modify
// ignore_for_file: unused_import, unused_field, unused_element, unused_local_variable

library;

import 'dart:typed_data' show Uint8List;

import 'package:flat_buffers/flat_buffers.dart' as fb;


class Stats {
  Stats._(this._bc, this._bcOffset);

  factory Stats(List<int> bytes) {
    final rootRef = fb.BufferContext.fromBytes(bytes);
    return reader.read(rootRef, 0);
  }

  static const fb.Reader<Stats> reader = _StatsReader();

  final fb.BufferContext _bc;
  final int _bcOffset;

  int get hp => const fb.Int32Reader().vTableGet(_bc, _bcOffset, 4, 100);

  int get mana => const fb.Int16Reader().vTableGet(_bc, _bcOffset, 6, 50);

  double get speed => const fb.Float32Reader().vTableGet(_bc, _bcOffset, 8, 1.5);

  bool get active => const fb.Int8Reader().vTableGet(_bc, _bcOffset, 10, 0) != 0;

  @override
  String toString() {
    return 'Stats{hp: ${hp}, mana: ${mana}, speed: ${speed}, active: ${active}}';
  }

  StatsT unpack() => StatsT(hp: hp?.unpack(), mana: mana?.unpack(), speed: speed?.unpack(), active: active?.unpack());

  static int pack(fb.Builder fbBuilder, StatsT? object) {
    if (object == null) return 0;
    return object.pack(fbBuilder);
  }
}

class _StatsReader extends fb.TableReader<Stats> {
  const _StatsReader();

  @override
  Stats createObject(fb.BufferContext bc, int offset) =>
    Stats._(bc, offset);
}

class StatsBuilder {
  StatsBuilder(this.fbBuilder);

  final fb.Builder fbBuilder;

  void begin() {
    fbBuilder.startTable(4);
  }

  int addHp(int hp) {
    fbBuilder.addInt32(0, hp);
    return fbBuilder.offset;
  }

  int addMana(int mana) {
    fbBuilder.addInt32(1, mana);
    return fbBuilder.offset;
  }

  int addSpeed(double speed) {
    fbBuilder.addInt32(2, speed);
    return fbBuilder.offset;
  }

  int addActive(bool active) {
    fbBuilder.addInt32(3, active);
    return fbBuilder.offset;
  }

  int finish() {
    return fbBuilder.endTable();
  }
}

class StatsT implements fb.Packable {
  int hp;
  int mana;
  double speed;
  bool active;

  StatsT({
    this.hp = 0,
    this.mana = 0,
    this.speed = 0.0,
    this.active = false
  });

  @override
  int pack(fb.Builder fbBuilder) {
    fbBuilder.startTable(4);
    fbBuilder.addInt32(0, hp);
    fbBuilder.addInt32(1, mana);
    fbBuilder.addInt32(2, speed);
    fbBuilder.addInt32(3, active);
    return fbBuilder.endTable();
  }

  @override
  String toString() {
    return 'StatsT{hp: ${hp}, mana: ${mana}, speed: ${speed}, active: ${active}}';
  }
}

class StatsObjectBuilder extends fb.ObjectBuilder {
  final int _hp;
  final int _mana;
  final double _speed;
  final bool _active;

  StatsObjectBuilder({
    this._hp,
    this._mana,
    this._speed,
    this._active,
  });

  @override
  int finish(fb.Builder fbBuilder) {
    fbBuilder.startTable(4);
    fbBuilder.addInt32(0, _hp);
    fbBuilder.addInt32(1, _mana);
    fbBuilder.addInt32(2, _speed);
    fbBuilder.addInt32(3, _active);
    return fbBuilder.endTable();
  }

  @override
  Uint8List toBytes([String? fileIdentifier]) {
    final fbBuilder = fb.Builder(deduplicateTables: false);
    fbBuilder.finish(finish(fbBuilder), fileIdentifier);
    return fbBuilder.buffer;
  }
}
